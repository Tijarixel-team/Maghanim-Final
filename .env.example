# Where contact form submissions are delivered. Destination is still an open
# decision — see PRODUCT.md. Without it the form validates and succeeds but
# nothing is sent, and the server logs a warning.
CONTACT_WEBHOOK_URL=

# Absolute origin, used for canonical URLs, hreflang and Open Graph.
NEXT_PUBLIC_SITE_URL=https://example.invalid
